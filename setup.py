from setuptools import setup, find_packages

setup(
    name="tesorai_search",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # dependencies go here
        "pandas",
    ],
)
